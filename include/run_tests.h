#ifndef RUN_TESTS_H
#define RUN_TESTS_H

/**
 * @brief Runs all unit tests in the system.
 *
 * This function executes tests for the main components of the system:
 * - runStateTests() – tests the system's state management
 * - runWorldTests() – tests the world representation
 * - runGraphTests() – tests graph structures and algorithms
 * - runPlannerTests() – tests the planning functionality
 */
void runAllTests();

#endif // RUN_TESTS_H